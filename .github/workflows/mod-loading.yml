name: Mod loading

on:
  pull_request:
    paths:
      - ".github/workflows/mod-loading.yml"
      - "go.mod"
      - "go.sum"
      - "cmd/client/**"
      - "cmd/server/**"
      - "internal/content/**"
      - "internal/distribution/**"
      - "internal/game/simulation/**"
      - "internal/mod/d2legacy/**"
      - "internal/modcache/**"
      - "internal/paths/**"
  schedule:
    - cron: "31 9 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mod-loading-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cross-platform:
    name: Cache and resolution (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Verify mod cache and distribution boundaries
        run: go test -count=1 ./internal/modcache ./internal/distribution ./internal/content
      - name: Verify embedded Lua and durable local files
        run: go test -count=1 ./internal/mod/d2legacy ./internal/mod/d2legacy/adapter/save ./internal/game/simulation ./internal/paths
